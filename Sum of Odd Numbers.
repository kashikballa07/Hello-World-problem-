#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
#include<stdio.h>
int main(){
    int i, N,sum;
    scanf("%d",&N);
    sum=0;
    for(i=1;i<=N;i++){
        if(i%2!=0){
            sum=sum+i;
        }
    }
    printf("%d",sum);
    
    

return 0;
}
