#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int main(){
    int n;
    scanf("%d",&n);
    int list[n];
    for(int i=0;i<n;i++){
        scanf("%d",&list[i]);
    }
    int val,flag=0;
    scanf("%d",&val);
    for(int i=0;i<n;i++){
        if(list[i]==val){
            flag=1;
            printf("Found");
            break;
        }
    }
    if(flag==0){
        printf("Not Found");
    }
    return 0;
}
