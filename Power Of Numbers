#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
#include <stdio.h>
#include <math.h>   // for pow()

int main() {
    int a, b;
    double result;

    scanf("%d %d", &a, &b);

    result = pow(a, b);

    printf("%.0lf\n", result);

    return 0;
}
