#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int main(){
    int num,num1,rev=0,rem;
    printf("enter n:");
    scanf("%d",&num);
    num1=num;
    while(num!=0){
        rem=num%10;
        rev=rev*10+rem;
        num=num/10;
    }
    if(num1 == rev){
        printf("Palindrome");
    }
    else{

        printf("Not Palindrome");
    }

    return 0;
}

    

